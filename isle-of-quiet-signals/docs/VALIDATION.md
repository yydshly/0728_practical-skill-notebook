# 验证报告

验证日期：2026-07-28
本地地址：`http://127.0.0.1:4173`

## 自动化结果

| 命令 | 结果 |
| --- | --- |
| `npm run validate:assets` | 通过 |
| `npm test` | 6 个文件、19 项测试通过 |
| `npm run test:browser` | 5 项 Playwright 端到端测试通过 |
| `npm run build` | 通过；产物为 HTML 5.26 kB、CSS 9.60 kB、JS 10.19 kB（均为未压缩显示值） |

最大单个运行时 WebP 为 `public/assets/scene/00-sky.webp`（83,844 B）；路线图中最大为 `keeper-house.webp`（71,396 B）。

## 真实浏览器检查

| 检查项 | 结果 |
| --- | --- |
| 1440 × 900 | 首屏构图、两段叙事、复焦与档案已检查；无横向溢出。 |
| 1280 × 720 | 档案详情按钮位于卡片可点击区域；真实点击可展开。 |
| 1024 × 768 | 灯塔、标题、页头导航保持可读；无横向溢出。 |
| 768 × 1024 | 经窄屏焦点修正后，完整灯塔仍在构图内。 |
| 390 × 844 | 灯塔、导航、档案卡片、按钮与拖拽均可用；无横向溢出。 |
| 200% 缩放约束 | Playwright 以 `720 × 450` 有效 CSS 视口（1440 × 900 在 200% 缩放下的布局约束）检查首屏和最终档案，均无横向溢出。应用内浏览器未暴露原生缩放控制。 |

时间轴检查点 `p=0, .18, .27, .44, .58, .74, .90, 1` 已由 Playwright 覆盖；其中 `.27` 守灯人面板、`.58` 雾中信号、`.74` 复焦、`.90` 档案进入与 `1` 最终档案另经应用内真实滚动截图复查。自 `p=1` 反向滚动回 `.58` 时，档案状态正确撤销，雾中信号面板和模糊焦点正确恢复。

## 交互、可访问性与降级

- 页头三段导航跳转到真实局部滚动进度，首屏导航已在真实浏览器点击复查。
- 航线档案的前后按钮、左右方向键、Home、End、详情展开、鼠标拖拽和手机触摸拖拽均经过自动化或真实浏览器操作验证；状态会更新实时播报文本。
- `prefers-reduced-motion: reduce` 端到端测试通过：舞台改为文档流，所有内容和档案控制器可达。
- 强制阻断灯塔关键层的端到端测试通过：失败图层获得 `data-failed` 标记并隐藏，深蓝舞台和标题仍保留。
- 应用内浏览器在最终复查时的 error/warning 控制台记录为空。

## 证据与限制

可复现的自动化证据位于 `tests/browser/`：`cinematic.spec.js`、`archive.spec.js`、`reduced-motion.spec.js`、`asset-fallback.spec.js`、`zoom.spec.js`。真实浏览器截图保留在本 Codex 任务的交互式浏览器会话中；未向仓库写入临时截图、trace 或报告文件。

已知素材限制见 [ASSETS.md](ASSETS.md)：四张路线图是可替换的母场景裁切临时图，等待正式独立路线素材。
