{
  "name": "isle-of-quiet-signals",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run --pool=forks --maxWorkers=1 --no-file-parallelism",
    "test:watch": "vitest --pool=forks --maxWorkers=1 --no-file-parallelism",
    "test:browser": "playwright test",
    "validate:assets": "node scripts/validate-assets.mjs"
  },
  "type": "module",
  "private": true,
  "description": "A scroll-driven cinematic lighthouse experience.",
  "devDependencies": {
    "@playwright/test": "^1.62.0",
    "jsdom": "^29.1.1",
    "vite": "^8.1.5",
    "vitest": "^4.1.10"
  }
}
